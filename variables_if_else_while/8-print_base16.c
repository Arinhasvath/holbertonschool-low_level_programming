#include <stdio.h>
#include <stdio.h>
/**
* main - Entry point
*
* Description: ll
*
*
* Return: Always 0 (Success)
*/
int main(void)
{
	int num;
	int alpha;

	for (num = '0'; num <= '9'; num++; alpha = 'a'; alpha <= 'f'; alpha++)
	{
		putchar(num alpha);
	}
	putchar('\n');
	return (0);
}
